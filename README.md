# NayAI
A new web non-DB projects that able to convert AI image to show pixelated colors without using AI detection system. Purely in TypeScript and Canvas ctx.

Right now, only core functionality is deployed. UI and secondary pages pending. 
